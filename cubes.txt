//to display the output of any number
#include<stdio.h>                                    //header files
int main(){                                          //main function
    float number;                                    //variable type
    printf("enter any number");                      //taking input from the user
    scanf("%f",&number);                             
    printf("the volume is:%f",number*number*number);   //main output
    return 0;
}
