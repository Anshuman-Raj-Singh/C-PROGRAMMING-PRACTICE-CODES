//algorithm to display 1 and 0 if and not if
#include<stdio.h>
int main(){
    int num;
    printf("enter the number:");
    scanf("%d",&num);
    (1<num,num<100)? printf("1") : printf("0");
    return 0;
} 
